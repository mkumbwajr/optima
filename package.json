{
  "name": "optima-employee-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An interactive CRDB Optima employee portal redesign, based on supplied screenshots.",
  "scripts": { "start": "node server.js", "dev": "node --watch server.js", "build": "node scripts/build.js", "test": "node --test tests/*.test.js", "check": "node --check public/app.js && node --check public/model.js && node --check server.js && node --check scripts/build.js" },
  "engines": { "node": ">=20" }
}
